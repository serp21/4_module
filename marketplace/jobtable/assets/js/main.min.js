function changeToSearchPosition(e) {
  console.log(e);
}
document.addEventListener("DOMContentLoaded", () => {
  document.querySelector("#jobtable-app").querySelector("[data-filter]");
  $("[data-close-popup]").on("click", (e) => {
    $(e.currentTarget).parents("[data-popup]").removeClass("open");
  }),
    $("[data-filter]")
      .off("click")
      .on("click", (e) => {
        $("[data-type='filter']").addClass("open");
      }),
    $("[data-settings]")
      .off("click")
      .on("click", (e) => {
        $("[data-type='settings']").addClass("open");
      }),
    $("[data-add-group]")
      .off("click")
      .on("click", (e) => {
        e.preventDefault(), $("[data-type='add-group']").addClass("open");
      }),
    $("[data-add-position]")
      .off("click")
      .on("click", (e) => {
        e.preventDefault(), $("[data-type='add-position']").addClass("open");
      }),
    $("[data-first-settings]")
      .off("click")
      .on("click", (e) => {
        e.preventDefault(), $("[data-type='first-settings']").addClass("open");
      }),
    $("#search-position")
      .off("select2:select")
      .on("select2:select", changeToSearchPosition),
    $("[data-checkbox-hidden")
      .on("change", (e) => {
        e.currentTarget.checked
          ? $("[data-element-hidden").addClass("active")
          : $("[data-element-hidden").removeClass("active");
      });
});
//# sourceMappingURL=../sourcemaps/main.min.js.map
